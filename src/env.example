# For local development, no VITE_API_URL is needed (defaults to http://localhost:3000).
# For production (Vercel), set this to your deployed backend URL:
# VITE_API_URL=https://your-backend-name.onrender.com
